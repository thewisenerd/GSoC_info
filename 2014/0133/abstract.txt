The main idea behind the proposal is to get NetSurf to KolibriOS to improve the user-experience as well as to improve the existing build system for HLL such as C/C++ so that future code that needs to be ported can be done with (even more) ease.