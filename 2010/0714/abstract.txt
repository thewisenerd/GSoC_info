To convert the encoded GIMPLE IR in the bytestream to a fixed format textual representation of the GIMPLE tuples and develop a front end for recognizing this textual GIMPLE, built on top of the LTO FE.