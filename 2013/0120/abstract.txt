GitLab is an open source git repository management application built with Ruby on Rails. It is used by many companies as their internal git management repository.  

The goal is to package GitLab and all its dependencies for Fedora.