Bring the project to the state of 2.0, using SIMD code, multithread-ready, much better cache utilization and SoA (structure of arrays) patterns.

Traversal of Nodes & Entities will be refactored to avoid redundant transforms and cache misses.