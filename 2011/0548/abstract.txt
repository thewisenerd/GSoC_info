The goal of the project is to fully document the QofObject, QofInstance, QofSession, QofBook and kvp_frame entities in LIBQOF module of GnuCash by means of unit tests from the point of view of developer to allow upcoming refactoring.