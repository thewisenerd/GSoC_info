The User defined functions  scan tables and return a single value  per row processed. Extending this functionality to scanning and returning a table, filtering tables and returning  specific rows or columns  from custom code(user defined function) will add more power to user defined functions API.  This project will provide a mini storage engine capable of handling user defined functions that can return a result set.