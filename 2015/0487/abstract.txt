RTEMS is mainly designed for Real time applications, embedded systems that implements a single process, multithreaded environment. The project focuses on the improvements of the BSP for Beagleboard that  various peripherals support like ADC,GPIO,I2C,USB . Future task would be to  fix the failing tests over qemu emulator and on the hardware running using JTAG, followed by automatic test run.